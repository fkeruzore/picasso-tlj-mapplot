[project]
name = "picasso-tlj-mapplot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "healpy",
    "matplotlib",
    "h5py",
    "cmocean>=4.0.3",
]

[dependency-groups]
dev = [
    "ruff>=0.15.6",
]
